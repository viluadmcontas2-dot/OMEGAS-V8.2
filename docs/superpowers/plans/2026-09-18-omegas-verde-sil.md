# OMEGAS Verde SIL Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans task-by-task. TDD applies to production-facing seams.

**Goal:** Build a deterministic headless replay laboratory that executes the real Verde MP48 decoder, engine analyzer and learning memory over recorded sessions, optimizing residual correction/STFT toward 0%, with ±5% only as the owner's operational acceptance band.

**Architecture:** Introduce only two production-neutral seams required for headless execution: an MP48 transport interface and a runtime clock interface, both defaulting to current Android behavior. The SIL itself lives under `tools/omegas-sil` plus JVM test fixtures; Python handles corpus reconstruction/dedup/orchestration, while Kotlin executes production science.

**Tech Stack:** Kotlin/JVM 2.0.21, existing Android/Gradle project, JUnit 4, Python 3 standard library, AgentRed/GitHub Issues.

**Spec:** `docs/superpowers/specs/2026-09-18-omegas-verde-sil-design.md`

## Global Constraints

- Base authority: current remote `OmegasVerde`.
- Scientific algorithm must not be copied into Python.
- No Android Emulator, APK, UI or ADB in this flow.
- Source corpus is read-only under `G:\Meu Drive\OMEGAS`.
- Scientific optimization target is 0% residual correction/STFT across usable conditions.
- Absolute correction <= 5% is operationally acceptable, but values inside the band remain candidates for improvement toward zero.
- Generalization/coverage/robustness constrain the optimization; they do not replace the zero-correction target.
- No ECU writer action.
- Remote GitHub remains source mutation authority.

---

### Task 1: Extract transport and clock seams without behavior change

**Files:**
- Create: `app/src/main/java/com/omegas/prohub/ecu/Mp48RuntimeClock.kt`
- Create: `app/src/main/java/com/omegas/prohub/usb/Mp48Transport.kt`
- Modify: `app/src/main/java/com/omegas/prohub/usb/UsbSerialManager.kt`
- Modify: `app/src/main/java/com/omegas/prohub/ecu/ResponseDrivenEcuEngine.kt`
- Test: `tests/test_mp48_serial_scheduler_behavior.py`
- Test: `app/src/test/java/com/omegas/prohub/ecu/ResponseDrivenEcuEngineTransportTest.kt`

**Interfaces:**
- `Mp48Transport.connected: Boolean`
- `Mp48Transport.protocolTransaction(...): UsbProtocolReply`
- `Mp48Transport.purge(reason: String)`
- `Mp48RuntimeClock.elapsedRealtime(): Long`
- `Mp48RuntimeClock.sleep(ms: Long)`

- [ ] Add a failing JVM test proving the engine accepts a deterministic fake transport and clock while preserving handshake -> telemetry order.
- [ ] Run the focused test and verify RED because the seams do not exist.
- [ ] Add the interfaces and default Android implementations.
- [ ] Make `UsbSerialManager` implement `Mp48Transport`; change only the engine constructor type and clock calls.
- [ ] Run existing scheduler/backpressure/protocol tests plus the new test and verify GREEN.
- [ ] Inspect diff: no handshake/recovery/timing policy changes.

### Task 2: Build validated corpus reconstruction and dedup

**Files:**
- Create: `tools/omegas-sil/corpus.py`
- Create: `tools/omegas-sil/test_corpus.py`
- Create: `tools/omegas-sil/README.md`

**Interfaces:**
- `iter_sources(root: Path) -> Iterable[Source]`
- `reconstruct_portmon(path: Path) -> list[RecordedTransaction]`
- `read_session_zip(path: Path) -> list[RecordedTransaction]`
- `canonicalize_sessions(...) -> CanonicalCorpus`

- [ ] Write fixtures/tests for the real MP48 envelope: echo command, status 0x53, length 0x22, 34-byte payload, checksum.
- [ ] Verify RED.
- [ ] Implement reconstruction using the protocol contract already validated against LOGNOVO.
- [ ] Fingerprint normalized frame streams and collapse exact duplicate sessions while retaining aliases/provenance.
- [ ] Verify LOGNOVO reconstruction count gate (20,450 valid telemetry transactions when that archive is present).
- [ ] Add output manifest with canonical session count, aliases, frame count, fuel distribution and parse failures.

### Task 3: Create headless replay harness over the real engine/analyzer/memory

**Files:**
- Create: `app/src/test/java/com/omegas/sil/RecordedMp48Transport.kt`
- Create: `app/src/test/java/com/omegas/sil/OmegasSilReplayTest.kt`
- Create: `tools/omegas-sil/run.py`

**Interfaces:**
- Input: NDJSON transactions generated by `corpus.py`.
- Output: `frames.ndjson`, `summary.json`, `learning.json`.
- Test system properties: `omegas.sil.input`, `omegas.sil.output`.

- [ ] Write RED test with 2 known 34-byte frames and expected decoded MAP/Petrol values.
- [ ] Implement `RecordedMp48Transport` that answers INIT/IDENTIFY/TELEMETRY deterministically and reports connected=true.
- [ ] Run real `ResponseDrivenEcuEngine` with fake clock and real callback.
- [ ] Feed each real `SampleDecision` into `MotorLearningMemory` using a temporary state file.
- [ ] Emit one normalized trace row per telemetry frame with decoded values, decision state, eligibility, learning state, comparison/reference metadata and sequence id.
- [ ] Stop exactly at corpus exhaustion without writer calls.
- [ ] Verify deterministic replay by hashing normalized outputs across two runs.

### Task 4: Add zero-target correction metrics, ±5% acceptance band, and Verde stability/advisor projection

**Files:**
- Create: `tools/omegas-sil/metrics.py`
- Create: `tools/omegas-sil/test_metrics.py`
- Extend: `app/src/test/java/com/omegas/sil/OmegasSilReplayTest.kt`

**Interfaces:**
- `relative_error_pct(observed, predicted)`
- signed `correction_pct` with target 0%
- mean/median/P90/P99 absolute correction and signed bias
- `within_5pct_rate` as acceptance-band metric
- session-level MAE/P90/P99/coverage
- stale/freeze counters

- [ ] Write RED metrics tests around exact 5% boundary.
- [ ] Compute reference error only where the real runtime produced a usable reference.
- [ ] Optimize and report distance to 0% correction across all usable samples, including samples already within ±5%.
- [ ] Report `within_5pct_rate` as the operational acceptance-band score, while retaining signed bias, absolute-correction distribution and MAE/P90/P99.
- [ ] Export real comparisons and run the existing Verde stability/advisor functions over them where their production APIs permit direct JVM use.
- [ ] Never count predicted/interpolated references as physical gasoline evidence.
- [ ] Tag failures by regime: stale/freeze, transient, high-load, insufficient support, other.

### Task 5: AgentRed corpus run and validation

**Files:** no source mutation during execution.

- [ ] AgentRed downloads/reads remote branch SHA into ephemeral runtime only.
- [ ] Run focused SIL tests.
- [ ] Run existing `test_mp48_serial_scheduler_behavior.py`, scientific runtime contract, and relevant Gradle unit tests.
- [ ] Enumerate/deduplicate all ZIPs under `G:\Meu Drive\OMEGAS`.
- [ ] Replay every canonical session headlessly, parallelizing independent sessions within host capacity.
- [ ] Produce aggregate `sil_fleet_summary.json` plus per-session artifacts.
- [ ] Run leave-one-session-out and blocked temporal summaries over canonical sessions.
- [ ] Publish AgentRed receipt with exact branch SHA, canonical sessions, frames, mean/median/P90/P99 absolute correction, signed bias, within-5% rate, MAE/P90/P99, coverage, stale/freeze rate and failures.
- [ ] Re-read remote branch HEAD and compare against base to ensure only SIL/seam changes exist.

## Completion Gate

Do not call the SIL ready from code review alone. Required fresh evidence is: focused tests GREEN, existing affected tests GREEN, deterministic replay GREEN, LOGNOVO count/parity gate GREEN, all canonical corpus sessions processed or explicitly classified, and an AgentRed receipt bound to the exact remote SHA.
