# Predictor Phase07 Steps 157–164 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Finish the software-side Predictor/Suggestions Fast-to-Zero path from revision-driven computation through G4B validation without fabricating RK3326 or physical held-out evidence.

**Architecture:** Keep Predictor scientific components pure and typed in `learning/`; Android/JSONObject stays at projection boundaries. One existing calibration Store/Router remains authoritative. Every step starts RED, gets an exact remote SHA verification, and closes independently in Linear only after audit/meta-audit.

**Tech Stack:** Kotlin/JVM, Android existing `org.json` projection, Node for WebView contract tests, deterministic host validation scripts.

**Spec:** Notion `FASE 07 — 147–164 — Predictor e Suggestions`, Steps 157–164.

## Global Constraints
- GitHub remote is source authority; Linear is execution writer.
- No GitHub Actions.
- Prediction != Observation; IdealTarget != StepPolicy; Suggestion lifecycle != estimator state.
- No second Store, Router, visual scheduler, serial scheduler, writer or polling loop.
- No physical/RK3326 PASS claim without real evidence.

---

### Task 157: Scientific revision gate and incremental plan
**Files:** create `PredictorRevisionEngine.kt`; create `PredictorRevisionEngineTest.kt`.
**Interfaces:** input `PredictorScientificRevisionKey` + dirty cells; output `NOOP|PATCH|FULL` plan and stable token.
- [ ] Write RED for 100k identical requests, one-cell evidence patch and global geometry full recompute.
- [ ] Verify RED.
- [ ] Implement typed revision key covering evidence/reference/calibration/geometry/physics/modelCalibration/sensitivityG.
- [ ] Run property/performance probe; ensure no JSON/string hot-loop dependency.
- [ ] Audit and close VIT-313.

### Task 158: Latest-revision coalescer and route independence
**Files:** create `PredictorLatestRevisionWorker.kt`; tests; modify Predictor UI route hook only if needed.
**Interfaces:** bounded single-pending work slot; generation token; metrics requested/computed/coalesced/superseded/maxComputeMs/maxQueueAge.
- [ ] RED race N/N+1/N+2 and stale completion publication.
- [ ] Implement latest-only coalescing/cancellation contract with no serial scheduler import.
- [ ] Remove route/context-triggered science refresh; route mount reads Store only.
- [ ] Run 100 mount/unmount + slow consumer contract.
- [ ] Audit and close Step158.

### Task 159: Suggestion lifecycle
**Files:** create `PredictorSuggestionLifecycle.kt`; test exhaustive transitions.
**Interfaces:** immutable suggestion record and explicit event reducer.
- [ ] RED exhaustive valid/invalid transition matrix including APPLIED only after confirmed mutation.
- [ ] Implement CANDIDATE→REVIEWABLE→ACTIONABLE→...→REVALIDATING→terminal outcome lifecycle.
- [ ] Prove UI churn is not an event and no after-data stays REVALIDATING.
- [ ] Audit and close Step159.

### Task 160: Real-outcome sensitivity g and calibration
**Files:** create `PredictorSensitivityCalibration.kt`; tests.
**Interfaces:** comparable before/after intervention evidence → local `gHat`, variance/provenance, model-error update or ABSTAIN.
- [ ] RED known-g recovery, tiny denominator, sign reversal, corrupted context/identity.
- [ ] Implement robust bounded posterior update preserving prior+variance and no plateau assumption.
- [ ] Prove contradiction inflates uncertainty/downgrades model before another correction.
- [ ] Audit and close Step160.

### Task 161: Active-learning value of information
**Files:** create `PredictorActiveLearning.kt`; tests/Monte Carlo host validator.
**Interfaces:** eligible natural operating regions → ranked `NEXT_USEFUL_REGION` diagnostic.
- [ ] RED duplicate-anchor penalty, new-span preference and 10k same-dwell non-dominance.
- [ ] Implement explicit VOI factors and acquisition-cost denominator; no driver instruction fields.
- [ ] Run random-anchor Monte Carlo + exhaustive k=3/4 regret comparison.
- [ ] Audit and close Step161.

### Task 162: Evidence-ingress firewall
**Files:** create `PredictorEvidenceIngress.kt`; tests.
**Interfaces:** source type + provenance → accepted real evidence or typed rejection.
- [ ] RED Prediction/Suggestion/Draft/UI injection and restore-cache cases.
- [ ] Implement allowlist for direct real observation/post-write outcome/historical prior with historical-not-current semantics.
- [ ] Prove 1,000,000 recomputes do not change support.
- [ ] Audit and close Step162.

### Task 163: Revalidation and time-to-zero metrics
**Files:** create `PredictorRevalidation.kt`; tests.
**Interfaces:** confirmed applied intervention + comparable after evidence → lifecycle outcome, metrics and adaptation input.
- [ ] RED 4→6 fast path, REGRESSED path, no-after REVALIDATING and metric monotonicity.
- [ ] Implement required product metrics without optimizing for raw data count.
- [ ] Run synthetic before→prediction→writer→after loops and convergence falsifier.
- [ ] Audit and close Step163.

### Task 164: G4B validation harness
**Files:** create/version `PredictorFastToZeroValidation.kt` plus deterministic validation runner/manifest.
**Interfaces:** frozen dataset/seeds/folds/config → scorecard with explicit software/physical/HW evidence fields.
- [ ] RED validator that refuses PASS with missing physical held-out or HW-001 evidence.
- [ ] Implement 13-battery software harness: properties, identity fuzz, nested LOSO/LOEO, anchor Monte Carlo, uncertainty, risk-coverage, ablations, adversarial drift, causal outcome, concurrency, performance-host, E2E, physical-evidence gate.
- [ ] Run all host-capable batteries and freeze thresholds before held-out evaluation.
- [ ] Probe ALEXA only if uniquely useful for Android build/HW evidence.
- [ ] Set G4B PASS only if real RK3326 + physical held-out evidence exists; otherwise close software implementation with explicit `PHYSICAL_GATE`/`INCONCLUSIVE`, never wording-based industrial PASS.
