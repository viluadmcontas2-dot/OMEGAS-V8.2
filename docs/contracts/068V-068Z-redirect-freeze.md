# 068V–068Z — Redirect contract freeze

Contract-only mirror of the binding 03→04 interfase. PASS here does not claim that the current Dashboard/UI/SessionBundle/Draft is already implemented.

## 068V — Dashboard primary metric parity
At 1280×720, RPM and Petrol Inj. occupy paired 50/50 primary metric blocks using the same `metric-primary-numeral` token, baseline and typographic weight. Initial mdpi-equivalent baseline: 48px numeral, 16–18px label. At 1024×600/split, both reduce together to an initial ~40px baseline. Fuel/freshness/collection remain secondary readable status. Future owner: F9 203/206/220/221/231. Proof requires viewport screenshot plus computed-style equality, not screenshot alone.

## 068W — Navigation preserves context and Draft
Preserve previous route, selected cell, applicable zoom, inspector state, individual DraftItems and global review. Learning/Predictor may edit contextually; full Map K is expansion, not a mandatory detour. Router changes presentation only and never causes scientific recomputation. Future owner: F9 201/207/209/211/223–228. Proof path: `Aprender → editar A → Predictor → editar B → Agora → Review`, preserving A+B.

## 068X — Mandatory autoaudit matrix
Before any owner declares `IMPLEMENTED_AWAITING_AUDIT`, attach at least: positive proof; independent falsifier; real producer→consumer call path; lifecycle/churn test for UI; schema/hash/time test for SessionBundle; range test for Draft; absence-of-writer-before-confirmation test; remote SHA/tree receipt. Screenshot/grep alone is never sufficient.

## 068Y — Dispatch; no duplicate architecture
- F4: 068H/068S/068T/068U outputs, reasons and serialization.
- F8: 068O/068P/068Q/068R plus semantic review from 068S.
- F9: 068C/068D/068L/068M/068N/068P/068S/068T/068U/068V/068W.
- F11: 068E/068F/068G/068H/068I/068J/068K/068M/068N plus long-session/import/export.
- F12: E2E session export→import, UI churn, independent Draft, overlay, bounds and didactics.
Each downstream receipt refers back to the canonical interfase instead of copying/diverging it.

## 068Z — Redirect gate 03→04
PASS means only:
1. contracts 068B–068Y are versioned;
2. downstream owners are identified;
3. F4 starts by producing the fields/reasons/serialization required downstream;
4. F8/F9/F11 cannot PASS while violating these contracts;
5. no parallel architecture was created.

Falsifier: if any material requirement lacks a producer, future owner or proof, `068Z=FAIL/PARTIAL` and 069 stays blocked.

## Current gaps confirmed at freeze time

- Current Dashboard renders RPM as a hero numeral while Petrol Inj. is smaller; 068V is not implemented yet.
- Current route is single-authority and persisted, but full Draft/inspector/zoom continuity required by 068W is not yet proven.
- GS-G2 must be revalidated at the redirect gate; host evidence never substitutes RK3326 physical latency/CPU validation.

No new Store, Router, Scheduler, serial path, Learning engine, Predictor authority or writer is authorized by this document.
