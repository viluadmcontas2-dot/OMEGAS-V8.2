# Canonical Atlas inputs

These files are the hash-bound source corpus for the ProgBase AutoCal reconstruction.

Do not replace, rename, normalize, edit, or recompress them without updating provenance and creating a new canonical-input manifest version.

The acquisition machine is transport only. Scientific authority is the remote blob plus the hashes in `atlas/manifests/canonical-inputs.json`.
